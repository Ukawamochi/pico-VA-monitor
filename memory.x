/* RP2040 標準メモリレイアウト */
MEMORY
{
  FLASH : ORIGIN = 0x10000000, LENGTH = 2048K
  RAM   : ORIGIN = 0x20000000, LENGTH = 264K
}

EXTERN(BOOT2_FIRMWARE);

SECTIONS
{
  .boot2 ORIGIN(FLASH) :
  {
    KEEP(*(.boot2));
  } > FLASH
}

PROVIDE(boot2 = BOOT2_FIRMWARE);

